import { takeEvery, takeLatest } from "redux-saga/effects";

// import { FETCH_VENUES } from "./venues/types";
// import { getHotelsSaga } from "../stores/venues/sagas";

// import {
//   checkCouponSaga,
//   newReservationSaga,
//   deleteReservationSaga,
// } from "stores/cart/sagas";

function* globalSagas() {
  // yield takeEvery(FETCH_VENUES, getHotelsSaga);
  // yield takeLatest(CHECK_COUPON, checkCouponSaga);
  // yield takeLatest(NEW_RESERVATION, newReservationSaga);
  // yield takeLatest(DELETE_RESERVATION, deleteReservationSaga);
}

export default globalSagas;
